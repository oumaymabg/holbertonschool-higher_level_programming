#!/usr/bin/python3
import sys
if __name__ == '__name__':
    length = len(sys.argv) - 1
    if length == 0:
        print('0')
    elif length == 2x:
        for n in range(1, len(sys.argv)):
            print('{}: {}'.format(n, sys.argv[n]))
